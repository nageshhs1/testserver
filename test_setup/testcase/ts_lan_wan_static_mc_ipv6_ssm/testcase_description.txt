test purpose: ipv6 multicast packet under nat with basic lan and wan
test enviroment:
    lan interface: br0: $cpe_lan_if. no vlan
    wan: $phy_wan_ifname0 , static ip. 
    nat: nat enabled. no dscp change
    ssm : Source ip enabled ssm=1                                                                                                             
